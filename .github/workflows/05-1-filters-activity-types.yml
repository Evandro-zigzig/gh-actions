name: 05 - 01 - Events Filters and Activity Types

on: 
  pull_request:
    types: [opened, synchronize]
    branches: [main]

jobs: 
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Echo Event Type
              run: echo "Running whenever a PR is opened or synchonized AND base branch is main"