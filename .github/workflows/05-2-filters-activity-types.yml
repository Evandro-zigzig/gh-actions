name: 05 - 02 - Event Filters and activity types

on: 
  pull_request:
    types:
      - closed
    branches: 
      - main

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Event Activity
        run: echo "Running whenever a PR is closed."     
